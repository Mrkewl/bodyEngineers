import 'package:localstorage/localstorage.dart';

class LocalStorageManager {
  final LocalStorage acountStorage = new LocalStorage('account.json');
  final LocalStorage storage = new LocalStorage('account.json');

  _saveAccount() {}
}
